# Byte-compiled / optimized / DLL files
__pycache__/
*.pyc
*o
*so

# C extensions
*so

# Distribution / packaging
build/
dist/
eggs/
*.egg-info/
*.egg

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.nox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover

# Environments
.env
.venv
env/
venv/
ENV/
env.bak
venv.bak
.env.*
!.env.example

# Docker
.dockerignore
docker-compose.yml.override

# Editor-specific settings
.vscode/
.idea/

# Jupyter Notebook checkpoints
.ipynb_checkpoints/

# --- Project Specific Ignores ---

# Data generated by the pipeline (should never be committed)
/data/

# DBT generated artifacts, logs, and compiled code
/dbt_project/logs/
/dbt_project/target/
/dbt_project/dbt_packages/
/dbt_packages/
/target/
/logs/

# DBT best practices: keep dbt_project.yml, models/, schema.yml, and packages.yml tracked
# Do NOT ignore dbt_project.yml, models/, schema.yml, or packages.yml
!dbt_project.yml
!dbt_project/models/
!dbt_project/models/**
!dbt_project/models/staging/schema.yml
!packages.yml

# Local database files
*.db
*.sqlite3

# Telegram session files (contain sensitive auth keys)
*.session

# dbt profiles.yml (usually not tracked; use a template if needed)
profiles.yml
!profiles.example.yml